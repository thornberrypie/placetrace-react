import React from 'react';

const CountryDropdown = (props) => {
  return (
    <select className="game-select" name="country">
      <option value="abc">ABC</option>
      <option value="123">123</option>
    </select>
  )
}

export default CountryDropdown;
